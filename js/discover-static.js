import {auth,db,collection,getDocs,query,orderBy,limit} from './firebase.js';
const grid=document.getElementById('discoverGrid');const search=document.getElementById('search');let items=[];
const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
function draw(){const q=(search?.value||'').toLowerCase().trim();const list=items.filter(v=>!q||`${v.title||''} ${v.creatorName||''} ${v.niche||''}`.toLowerCase().includes(q));if(!list.length){grid.innerHTML='<div class="empty-card"><h3>No creator posts yet</h3><p>New videos appear here when members submit them. Share Theophos with other creators so the community can grow.</p><a class="btn btn-primary" href="submit.html">Submit your video →</a></div>';return;}grid.innerHTML=list.map(v=>`<article class="quick-card"><b>${esc(v.title||'Creator video')}</b><span>${esc(v.niche||'Creator')} · ${esc(v.contentType||'Video')}<br>by ${esc(v.creatorName||'Creator')}</span><a href="support.html?id=${encodeURIComponent(v.id)}">Watch & support →</a></article>`).join('');}
async function load(){if(!grid)return;try{if(!auth?.currentUser){grid.innerHTML='<div class="empty-card">Sign in to discover creator content.</div>';return;}const snap=await getDocs(query(collection(db,'videos'),orderBy('createdAt','desc'),limit(60)));items=snap.docs.filter(d=>d.data().creatorId!==auth.currentUser.uid).map(d=>({id:d.id,...d.data()}));draw();}catch(e){console.error(e);grid.innerHTML='<div class="empty-card"><h3>We could not load Discover</h3><p>Please refresh. If this continues, check the Firebase connection.</p></div>';}}
search?.addEventListener('input',draw);if(auth)auth.authStateReady?.().then(load).catch(load);
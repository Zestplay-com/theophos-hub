import {auth,db,collection,getDocs,query,orderBy} from './firebase.js';
const grid=document.getElementById('myContentGrid');
const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
async function load(){if(!grid||!auth?.currentUser)return;try{const snap=await getDocs(query(collection(db,'videos'),orderBy('createdAt','desc')));const items=snap.docs.filter(d=>d.data().creatorId===auth.currentUser.uid).map(d=>({id:d.id,...d.data()}));if(!items.length){grid.innerHTML='<div class="empty-card">You have not submitted a video yet. <a href="submit.html">Submit your first video →</a></div>';return;}grid.innerHTML=items.map(v=>`<article class="quick-card"><b>${esc(v.title||'Creator video')}</b><span>${esc(v.niche||'Creator')} · ${esc(v.contentType||'Video')}<br>Submitted to Theophos Hub</span><div style="display:flex;gap:8px;flex-wrap:wrap"><a href="support.html?id=${encodeURIComponent(v.id)}">Open feedback page →</a><a href="${esc(v.youtubeUrl)}" target="_blank" rel="noopener">Open YouTube ↗</a></div></article>`).join('');}catch(e){console.error(e);grid.innerHTML='<div class="empty-card">Could not load your content. Please refresh and try again.</div>';}}
if(auth)auth.authStateReady?.().then(load).catch(load);
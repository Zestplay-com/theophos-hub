import './firebase-config.js';import{initializeApp}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js';import{getAuth,onAuthStateChanged}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js';const app=initializeApp(window.THEOPHOS_FIREBASE_CONFIG),auth=getAuth(app);let user=null;onAuthStateChanged(auth,u=>{if(!u)location.href='login.html';else user=u});document.getElementById('ask').onclick=async()=>{const q=document.getElementById('question').value.trim(),answer=document.getElementById('answer');if(!q){answer.textContent='Write your question first.';return}if(!user){answer.textContent='Please sign in first.';return}answer.textContent='The secure AI Coach endpoint will analyze this question when the Firebase server function is connected. Your API key will never be placed in this browser code.'};
const modal=document.getElementById('modal');const content=document.getElementById('modalContent');
function openModal(type){modal.classList.add('show');if(type==='signin'){content.innerHTML='<div class="eyebrow">WELCOME BACK</div><h2>Sign in to Theophos Hub</h2><p>This interface is ready for Firebase Authentication.</p><div class="form"><input type="email" placeholder="Email address"><input type="password" placeholder="Password"><button class="btn primary">Sign in</button></div>';}else{content.innerHTML='<div class="eyebrow">FOUNDING CREATOR</div><h2>Create your creator account</h2><p>We are rebuilding around a simple creator-first experience with Firebase.</p><div class="form"><input type="text" placeholder="Creator name"><input type="email" placeholder="Email address"><input type="password" placeholder="Password"><button class="btn primary">Create account</button></div>';}}
document.querySelectorAll('[data-open]').forEach(function(b){b.addEventListener('click',function(){openModal(b.dataset.open);});});document.querySelector('[data-close]').addEventListener('click',function(){modal.classList.remove('show');});modal.addEventListener('click',function(e){if(e.target===modal)modal.classList.remove('show');});
